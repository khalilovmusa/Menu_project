import * as React from 'react'

import styles from './LoginPage.module.css'

const LoginPage = (): React.JSX.Element => {
   return <div className={styles.loginpage_main}>Login page</div>
}

export default LoginPage
